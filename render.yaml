services:
  - type: web
    name: core-x
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class eventlet -w 1 server:app -b :10000
    repo: https://github.com/alpha-1-design/Core-x
    branch: main
    healthCheckPath: /health
    autoDeploy: no
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"